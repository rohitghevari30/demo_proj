# Level 8 — Prometheus scrape config
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "secureapp_backend"
    static_configs:
      - targets: ["app:5000"]

rule_files:
  - "rules/brute_force.yml"